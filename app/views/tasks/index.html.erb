<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.title, task_path(task)%> <%= check_box_tag("", "", task.completed) %></li>
    <%= link_to "Update Task", edit_task_path(task)%>
  <% end %>
</ul>


<%= link_to "New Task", tasks_new_path %>
